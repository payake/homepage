---
interface Props {
  id: string;
  title: string;
}

const { id, title } = Astro.props;
---
<section id={id} class="py-16">
  <div class="max-w-4xl mx-auto px-6">
    <h2 class="text-2xl font-bold mb-8 text-primary">{title}</h2>
    <slot />
  </div>
</section>